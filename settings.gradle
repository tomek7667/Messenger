pluginManagement {
    repositories {
        maven { url = 'https://maven.fabricmc.net/' } // Loom snapshots
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Messenger"
